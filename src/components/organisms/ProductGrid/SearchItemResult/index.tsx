import { SearchItemResultItems } from "./SearchItemResultItems";
import { SortBar } from "./SortBar";

export const SearchItemResult = () => {
  return (
    <div>
      <SortBar />
      <SearchItemResultItems />
    </div>
  );
};
